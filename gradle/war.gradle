apply plugin: "war"

bootWar {
    mainClassName = "com.vedio.remote.VedioApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
